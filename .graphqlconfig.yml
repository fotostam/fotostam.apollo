projects:
  app:
    schemaPath: src/schema.graphql
    extensions:
      endpoints:
        default: http://localhost:4000
  prisma:
    schemaPath: src/typeDefs/prisma.graphql
    extensions:
      prisma: database/prisma.yml

